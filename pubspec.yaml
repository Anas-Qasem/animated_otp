name: animated_otp_field
description: >-
  A highly customizable, animated OTP input field for Flutter with smooth
  digit-entry animations, shake feedback, haptic response, and built-in
  validation states.
version: 0.1.0
homepage: https://github.com/Anas-Qasem/animated_otp
repository: https://github.com/Anas-Qasem/animated_otp
environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  shake_animation_widget: ^3.0.4
  gaimon: ^1.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
