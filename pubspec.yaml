name: animated_otp_field
description: >-
  A highly customizable, animated OTP input field for Flutter with smooth
  digit-entry animations, shake feedback, haptic response, and built-in
  validation states.
version: 0.1.0
# TODO: Set your repository URL before publishing.
# homepage: https://github.com/<user>/animated_otp_field
# repository: https://github.com/<user>/animated_otp_field

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  shake_animation_widget: ^3.0.4
  gaimon: ^1.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
